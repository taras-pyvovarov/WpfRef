<Window x:Class="Shell.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Shell.Views"
        xmlns:prism="http://prismlibrary.com/"
        xmlns:common="clr-namespace:Common;assembly=Common"
        Title="App Main Window" SizeToContent="WidthAndHeight">

    <!--Root shell panel-->
    <DockPanel>

        <!--Bottom status panel-->
        <Border DockPanel.Dock="Bottom">
            <ContentControl prism:RegionManager.RegionName="{x:Static common:AppConstants.StatusRegion}"/>
        </Border>

        <DockPanel DockPanel.Dock="Left">

            <!--Logo panel-->
            <Border DockPanel.Dock="Top">
                <ContentControl prism:RegionManager.RegionName="{x:Static common:AppConstants.LogoRegion}"/>
            </Border>

            <!--Left helper panel-->
            <Border>
                <ContentControl prism:RegionManager.RegionName="{x:Static common:AppConstants.LeftRegion}"/>
            </Border>

        </DockPanel>

        <!--Main workspace-->
        <Border>
            <ContentControl prism:RegionManager.RegionName="{x:Static common:AppConstants.MainRegion}"/>
        </Border>
    </DockPanel>
</Window>
